import React from 'react';
import Layout from '@theme/Layout';

// This is the main App component
// It can be used to wrap the entire application with providers
// For now, we don't have any authentication requirement
export default function App() {
  return (
    <Layout>
      {/* The actual pages are handled by Docusaurus router */}
    </Layout>
  );
}